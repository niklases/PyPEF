[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pypef"
dynamic = ["version", "readme"]
authors = [
    { name = "Niklas Siedhoff", email = "niklas.siedhoff@rwth-aachen.de" }
]
description = """\
A tool for performing data-driven protein engineering by building, testing, and \
inferring machine learning models trained on protein variant fitness data.\
"""
requires-python = ">=3.10"
license = { text = "CC BY-SA 4.0" }
keywords = ["Pythonic Protein Engineering Framework"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Topic :: Scientific/Engineering :: Artificial Intelligence"
]
dependencies = [
    "numpy",
    "pandas",
    "tqdm",
    "docopt-ng",
    "matplotlib",
    "scipy",
    "adjustText",
    "scikit-learn",
    "biopython",
    "biotite",
    "schema",
    "pathos",
    "peft",
    "nvidia-ml-py",
    "gpytorch",
    "huggingface_hub[hf_xet]",
    "torch",
    "torch-geometric"
]
[project.optional-dependencies]
gui = ["pyside6"]

[tool.setuptools.dynamic]
version = {attr = "pypef.__version__"}
readme = {file = "README.md", content-type = "text/markdown"}

[project.urls]
Homepage = "https://github.com/niklases/PyPEF"

[project.scripts]
pypef = "pypef.main:run_main"
pypef-gui-cli = "pypef.gui.qt_window:run_app"

[project.gui-scripts]
pypef-gui = "pypef.gui.qt_window:run_app"

[tool.setuptools.packages.find]
where = ["."]
include = ["pypef*"]

[tool.setuptools.package-data]
pypef = [
    "ml/AAindex/*",
    "ml/AAindex/Refined_cluster_indices_r0.93_r0.97/*",
    "plm/prosst_structure/static/*",
    "gui/assets/*"
]

[tool.pytest.ini_options]
markers = [
    "main_script_specific",
    "pip_specific",
    "requires_gpu"
]
